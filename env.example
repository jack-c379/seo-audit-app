# Backend Environment Variables (for Render)
# Copy this to .env for local development

# Required: Google API Key for ADK agent
GOOGLE_API_KEY=your-google-api-key-here

# Optional: Firecrawl API Key for web scraping
FIRECRAWL_API_KEY=your-firecrawl-api-key-here

# CORS Configuration (for production deployment)
# Comma-separated list of allowed origins (frontend URLs)
# Example: https://your-frontend.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=https://your-frontend-domain.com

# Render automatically sets PORT - don't set this manually
# For local development, you can optionally set:
# PORT=8000
