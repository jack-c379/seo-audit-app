# Suppress funding messages (not errors)
fund=false

# Note: The path-match deprecation warning comes from react-scripts dependencies
# This is safe to ignore - it doesn't affect functionality
# react-scripts will eventually update their dependencies to remove deprecated packages

